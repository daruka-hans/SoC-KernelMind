To train the agent on 20000 episodes and generate plot, and q-table, run -
    python3 train.py

To run the trained agent on a single test episode completely exploiting(epsilon=0) based on the q-table and with a small animation of landing, run-
    python3 run.py
